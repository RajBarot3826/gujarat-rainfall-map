# Gujarat Rainfall Station Map

A quick geospatial project visualizing a rainfall station in Gujarat, India using Python and Folium.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the script:
   ```bash
   python main.py
   ```

3. Open the generated `india_climate_map.html` in your web browser.
